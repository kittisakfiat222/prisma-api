// import express from "express";
// import { login } from "../controllers/auth/authController"; // Import login controller
// import { authenticateJWT } from "../middlewares/authenticateJWT"; // Import JWT authentication middleware

// const router = express.Router();

// // Public routes
// router.post("/login", login);

// // Protected routes (Example)
// router.get("/profile", authenticateJWT, (req, res) => {
//   res.status(200).json({
//     status: "success",
//     message: "Profile data",
//     user: req.user, // Access user data from JWT
//   });
// });

// export default router;
